const config = {
    //启动窗口
    port: 3000,
    // 数据库配置
    database: {
        DATABASE: 'music_electron',
        USERNAME: 'root',
         PASSWORD: 'root',
        HOST: 'localhost',
        PORT: 3306
    }
};

module.exports = config;