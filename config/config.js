const config = {
  //启动窗口
  port: 3000,
  // 数据库配置
  database: {
    DATABASE: 'demo',
    USERNAME: 'root',
    PASSWORD: 'root',
    PORT: '3306',
    HOST: 'localhost'
  }
}

module.exports = config