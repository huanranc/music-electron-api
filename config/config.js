const config = {
    //启动窗口
    port: 3000,
    // 数据库配置
    database: {
        DATABASE: 'music_electron',
        USERNAME: 'root',
        PASSWORD: 'maxsky',
        HOST: '127.0.0.1',
        PORT: 3366
    }
};

module.exports = config;